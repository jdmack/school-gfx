#include "globals.h"
#include "camera.h"

namespace Globals
{
    Camera camera = Camera();
    Group * root;// = new Group();
    bool flag = false;
    bool show_bound = false;
    Matrix4 identity_matrix;
};
